# UI 设计变更记录

## M6 分享、评论与批注闭环 - 2026-08-24

- 图片详情接入登录下载、分享管理、所有者主动撤回、评论、回复、提及、解决/重新打开和软删除。
- 位置批注使用图片内容盒归一化坐标，当前版本显示编号锚点，历史版本仅保留讨论列表状态。
- 分享弹窗支持 1/7/30 天、自定义失效时间、永久有效、可选密码、一次性完整链接、撤销和重新生成。
- 新增 `/shares/[publicId]` 受控分享页，使用片段密钥换取 Session 授权，并加入密码、无效、到期、撤销、加载和登录续办状态。
- 登录续办只在查询参数中传递无 fragment 的分享路径，片段密钥暂存于当前标签页并在恢复后清理，不再进入登录页 URL 或访问日志。
- 分享页面和资源使用 noindex/no-referrer/no-store，浏览器只访问后端图片 URL。
- 讨论列表支持游标加载更多，提及候选使用结构化用户 ID，不解析正文昵称。
- Docker Compose 的 MySQL 8、Redis、MinIO 真实环境已完成 M6 验收；Flyway V17/V18、后端 101 项测试、前端 lint、类型检查、46 项 Vitest、生产构建和 M6 OpenAPI 解析均通过。
- 完整 Playwright 为 5 项业务流程通过、3 项可选视觉基线采集按开关跳过；管理员审核发布和审核后撤回均实际执行，临时管理员在测试后已恢复为普通用户。
- 图片详情与已授权分享页在 1440x900、1024x768、390x844 三视口的 6 张全页截图已人工走查，无横向溢出、控件越界或同父级控件重叠。
- 完整套件同时修正 M3 直接 API 断言缺少标签页 Session Context，以及文字工具切换时最后输入和对象属性被旧 Fabric 状态覆盖的问题。

## M3-R 编辑器生产闭环 - 2026-08-16

- 根据生产验收反馈完成 M3 重构，编辑器改用 Fabric.js MIT 作为交互内核。
- 图片所有者和平台管理员可从图片详情进入编辑器，个人空间卡片也提供直接编辑入口；访客和其他用户不展示按钮。
- 业务状态重做为 `EditorState v2`，不持久化 Fabric 私有 JSON。
- 工作区加入裁切工具、图层列表、对象属性、完整图片调节入口和统一保存状态。
- 顶部加入退出编辑入口和三动作确认：可保存草稿退出、不保存并回退本次会话，或继续编辑；退出固定返回图片详情页。
- 草稿加入 revision 乐观并发控制，自动保存、退出、正式版本保存和恢复串行提交，陈旧窗口不再覆盖新草稿。
- 优化图片调节滑块：连续滑动使用工作区分辨率预览并按动画帧合并底图更新，松手后只生成一条撤销记录和一次自动保存；全分辨率导出管线保持不变。
- 预览与 PNG 导出统一使用自有 Canvas 滤镜管线，裁切与 14 项图片调节（含高光、阴影和锐度）全部通过专项测试。
- 真实注册、MinIO 上传、完整编辑、版本保存/恢复 Playwright 流程通过；1440×900、1024×768、390×844 三视口人工走查通过。

## M3 单人编辑器与版本 - 2026-08-16

- `/editor/[pictureId]` 落地为深色单人编辑工作区，桌面左右分栏，移动端上下布局。
- 选择、画笔、马克笔、擦除、文字、旋转、缩放和撤销重做已可用；工具状态和计数保持稳定尺寸。
- 图片调节侧栏接入结构化参数，草稿 800ms 防抖自动保存，保存状态在顶部持续可见。
- 版本历史抽屉展示缩略图、来源、创建者和恢复入口；恢复后回到当前画布并保留后续历史。
- 版本预览和导出统一经后端鉴权资源接口，未引入前端假数据或第二套图片存储。

## M2 AI 创作闭环 - 2026-08-13

- `/ai/create` 和 `/ai/tasks` 已落地，模型、比例、清晰度和配额全部来自后端。
- 创建页使用参数面板 + 冷灰网格画布；任务中心桌面使用紧凑表格，移动端切换为任务列表。
- 增加 loading、empty、error、permission denied、取消反馈、失败原因和额度返还状态。
- 任务结果预览与下载只使用后端图片资源 URL，未引入前端假数据或第二套存储。

## v1.1 实现进度 - 2026-08-12

- 已将 M1 公共图库、图片详情、登录注册、个人空间、图片上传和公开审核页面落地为 v1.1 视觉。
- 公共页面使用顶部导航与图片优先瀑布流，业务与管理页面使用冰蓝灰侧栏工作台。
- 已补充页面进入、图片悬停、表单进入和预览切换动效，并适配减少动态效果偏好。
- 删除前端推算的浏览、点赞和空间容量指标，仅展示后端真实提供的数据。

## v1.1 - 2026-08-12

- 确立飞书式冰蓝灰、白色内容面和高识别蓝主色。
- 将紫色和渐变降为少量品牌/AI 场景的辅助表达。
- 登录后页面采用浅色侧边栏；编辑器保留深色工作区。
- 建立公开图库、详情、个人空间、上传、AI、编辑器、团队和审核页面产品图。
- 建立设计事实来源、Design Token、PR 检查和视觉回归流程。

## v1.0 - 2026-08-12

- 初始 UI 升级探索已被 v1.1 取代，文档从当前工作树删除，可通过 Git 历史追溯。
- 该版本的紫蓝渐变和新增前端依赖建议已被 v1.1 取代。
