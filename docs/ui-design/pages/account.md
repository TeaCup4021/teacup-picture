# 账号与会话

- 路由：`/login`、`/register`
- 里程碑：M1
- 状态：已实现
- 产品图：待补充

## 目标

完成登录、注册和 Cookie 会话恢复。当前登录后进入角色默认页；返回登录前页面尚未实现。

## 布局

- 桌面可使用真实图库图片作为一侧视觉区域，表单保持白色、清晰和克制。
- 手机隐藏辅助视觉，表单占据主要视口。
- 不制作营销型 Hero，不展示未实现产品能力。

## 表单与错误

- 标签始终可见，浏览器自动填充属性正确。
- 注册成功后进入登录页，不在浏览器保存 Token。
- 错误信息保留用户输入，不显示后端堆栈或敏感信息。
- 当前受保护页面展示无权限状态和登录入口。保存原 URL 并在登录后返回属于后续增强。
