-- 缓存失效不再需要事务发件箱。
--
-- 改用共享版本号之后，失效动作只有两条幂等的缓存命令：删除该图的详情键、版本号递增。
-- 递增是「至少一次」语义，并发或重复执行只会让版本号多跳几位，不会产生错误；
-- 通知丢失也只是延迟收敛，由缓存自身的有效期兜底，不会造成永久陈旧。
-- 因此原先为「失效意图与业务提交绑定、并保证投递」而建的这张表已无用途。
DROP TABLE IF EXISTS `cache_invalidation_outbox`;
